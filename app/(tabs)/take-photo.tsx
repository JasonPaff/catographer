import { Text, View } from 'tamagui';

export default function TakePhotoTab() {
    return (
        <View alignItems={'center'} flex={1} justifyContent={'center'}>
            <Text fontSize={20}>Take Photo Screen</Text>
        </View>
    );
}
